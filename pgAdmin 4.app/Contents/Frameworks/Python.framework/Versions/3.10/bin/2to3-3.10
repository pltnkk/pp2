#!/bin/sh
'''exec' "$(dirname "$0")/python3.10" "$0" "$@"
' '''
# the above calls the python3.10 interpreter relative to the directory of this script
import sys
from lib2to3.main import main

sys.exit(main("lib2to3.fixes"))
